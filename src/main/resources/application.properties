server.port = 8081

## Database Config
spring.datasource.url=jdbc:postgresql://localhost:5432/order-ms
spring.datasource.username=postgres
spring.datasource.password=0000

## Jpa Config
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

## RabbitMQ Config
spring.rabbitmq.addresses=

broker.exchange.order=order.exchange

broker.exchange.stock=stock.exchange

broker.routing.order-created=order.created

broker.queue.order.stock.reserved=order.stock.reserved.queue
broker.queue.order.stock.failed=order.stock.failed.queue



